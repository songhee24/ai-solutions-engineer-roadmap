name: Deploy to GitHub Pages

on:
  push:
    branches: [main]
  workflow_dispatch:

permissions:
  contents: read
  pages: write
  id-token: write

concurrency:
  group: pages
  cancel-in-progress: false

jobs:
  deploy:
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v7

      - name: Configure Pages
        uses: actions/configure-pages@v6

      # Собранный планнер в репозиторий не коммитится: иначе он молча
      # расходился бы с planner-src. Карта сборки по-прежнему не требует —
      # index.html/app.js/styles.css/roadmap-data.js выкладываются как есть.
      - name: Setup Node
        uses: actions/setup-node@v7
        with:
          node-version: 24
          cache: npm
          cache-dependency-path: planner-src/package-lock.json

      # Тесты гоняются ДО сборки: красные должны останавливать деплой.
      # Раньше их здесь не было вовсе, и на Pages мог уехать сломанный планнер.
      - name: Install planner deps
        run: npm ci
        working-directory: planner-src

      # Данные карты проверяются голым Node: в корне репозитория нет npm.
      - name: Test map data
        run: node --test "test/**/*.test.mjs"

      - name: Test planner
        run: npm test
        working-directory: planner-src

      - name: Build planner
        run: npm run build
        working-directory: planner-src

      - name: Upload artifact
        uses: actions/upload-pages-artifact@v5
        with:
          path: .

      - name: Deploy to GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v5
